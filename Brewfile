# Brewfile
# This file is used by Homebrew to manage packages.
# To install the packages listed in this file, run:
#   brew bundle
# To install without auto-updating Homebrew (plus verbose output), run:
#   HOMEBREW_NO_AUTO_UPDATE=1 brew bundle --verbose

# Infiscal CLI
tap "infisical/get-cli"
brew "infisical"
